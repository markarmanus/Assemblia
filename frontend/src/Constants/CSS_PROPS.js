import COMPONENT_TYPES from "./COMPONENT_TYPES";
import INPUT_TYPES from "./INPUT_TYPES";

const CSS_PROPS = {
  [COMPONENT_TYPES.BUTTON]: {
    backgroundColor: {
      type: INPUT_TYPES.COLOR_PICKER,
      data: {},
    },
  },
};

export default CSS_PROPS;
